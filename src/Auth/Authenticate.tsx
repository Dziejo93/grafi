import React from 'react';
import Login from './Login';

const Authenticate: React.FC = () => {
  // eslint-disable-next-line
  if (false) {
    return <Login />;
  }
  return <div>Eroino</div>;
};

export default Authenticate;
